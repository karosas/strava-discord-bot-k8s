apiVersion: v1
kind: Service
metadata:
  name: {{ include "strava-discord-bot-webui-service.fullname" . }}
  labels:
    {{- include "strava-discord-bot-webui-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "strava-discord-bot-webui-service.selectorLabels" . | nindent 4 }}
